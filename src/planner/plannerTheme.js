// src/planner/plannerTheme.js — KP-Planner 색상 팔레트(화이트 + 분홍 벚꽃 톤).
// 배차프로그램의 네이비 계열과는 완전히 다른, 이 앱만의 톤이다. 곳곳에 네이비가
// 남아있다는 피드백으로 전체를 이 팔레트로 통일했다 — 진한 포인트 색이 필요한
// 자리(버튼 배경, 헤더, 활성 탭 등)는 전부 PINK, 텍스트/아이콘처럼 대비가 필요한
// 자리는 네이비 대신 짙은 로즈(어두운 자주빛 회색) INK를 쓴다.
export const PINK = "#EC6FA0";       // 로고와 동일한 포인트 핑크
export const PINK_DARK = "#D8578E";  // 눌림/hover
export const PINK_SOFT = "#FDEEF5";  // 아주 옅은 분홍 배경
export const PINK_BORDER = "#F7D3E3"; // 옅은 분홍 테두리
export const INK = "#4A2E3D";        // 네이비를 대신하는 진한 로즈 차콜(본문/헤더 텍스트)
