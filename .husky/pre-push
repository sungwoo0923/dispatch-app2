#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🚀 버전 자동 증가 실행"

npm run version:auto

git add package.json public/version.json

# 변경 있을 때만 commit
git diff --cached --quiet || git commit -m "auto: version bump"